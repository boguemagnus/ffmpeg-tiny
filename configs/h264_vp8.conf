# H.264 MP4 + VP8 WebM — lighter software decode than VP9.
PRESET_SUMMARY="h264,vp8 · mov,matroska"
PRESET_ARGS=(
  --enable-decoder=h264,vp8
  --enable-parser=h264,vp8
  --enable-demuxer=mov,matroska
  --enable-bsf=h264_mp4toannexb
)
