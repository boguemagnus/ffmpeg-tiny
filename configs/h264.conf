# Minimal: H.264 in MP4/MOV (ISO-BMFF). Foundry packs almost always ship this.
PRESET_SUMMARY="h264 · mov"
PRESET_ARGS=(
  --enable-decoder=h264
  --enable-parser=h264
  --enable-demuxer=mov
  --enable-bsf=h264_mp4toannexb
)
